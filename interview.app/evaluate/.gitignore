# Intermediate agent-authored question files. They're staging output
# from harvest agents; the verified questions are merged into the
# canonical banks (sql.json / python.json / design.json) before commit
# and the staging files are deleted on the same commit.
data/*-additional.json
data/*-datalemur.json
data/*-leetlab.json
data/*-mohdbakr.json
data/*-debashis.json
data/*-rajen.json
data/*-production.json
