# PaddySpeaks Analytics Worker Configuration
# ──────────────────────────────────────────
# Setup steps:
#   1. npm install -g wrangler
#   2. wrangler login
#   3. wrangler d1 create paddyspeaks-analytics
#   4. Copy the database_id into this file below
#   5. wrangler d1 execute paddyspeaks-analytics --file=schema.sql
#   6. Generate your password hash:
#        echo -n "YOUR_PASSWORD" | sha256sum
#      Copy the hash into ADMIN_PASSWORD_HASH below
#   7. wrangler deploy

name = "paddyspeaks"
main = "worker.js"
compatibility_date = "2024-09-01"

[[d1_databases]]
binding = "DB"
database_name = "paddyspeaks-analytics"
database_id = "f295b6c5-41e7-4e9f-9cff-6c095ce5c785"

# ── Anonymous leaderboard — SEPARATE D1 db (no PII crossover with analytics) ──
# Schema:  wrangler d1 execute paddyspeaks-leaderboard --file=leaderboard-schema.sql
#          (or paste leaderboard-schema.sql into the D1 Console in the dashboard)
[[d1_databases]]
binding = "LB"
database_name = "paddyspeaks-leaderboard"
database_id = "d49bd1fd-0460-4339-b46d-94f00981a4ad"

# ── Contact + Testimonials — SEPARATE D1 db (holds PII: names/emails, so it is
#    isolated from the PII-free analytics DB, mirroring the leaderboard split) ──
# Schema applied from forms-schema.sql (comment-free copy for the D1 dashboard
# Console: forms-schema.console.sql).
[[d1_databases]]
binding = "FORMS"
database_name = "paddyspeaks-forms"
database_id = "d43111c5-5834-4791-b18d-b892643787c6"

# ADMIN_PASSWORD_HASH is set as a Secret in the Cloudflare dashboard.
# It also authorizes the testimonial moderation console (/testimonials/admin.html).
#
# Contact + Testimonials email (Resend — https://resend.com) uses these Secrets/Vars
# in the Cloudflare dashboard (Worker → Settings → Variables and Secrets):
#   RESEND_API_KEY      (Secret)  — Resend API key
#   CONTACT_FROM_EMAIL  (Secret)  — verified sender, e.g. "paddy@paddyspeaks.com"
#   CONTACT_TO_EMAIL    (Secret)  — the owner's private inbox (NEVER shipped to the browser)
#   FORMS_SALT          (Secret)  — optional; salt for one-way IP/content hashes
#
# ⚠ ALL FOUR MUST BE **SECRETS**, NOT PLAINTEXT VARIABLES.
# A Git-integrated deploy replaces dashboard plaintext vars with this file's
# [vars] block — which does not exist — so a plaintext var is silently WIPED on
# the next push. Secrets are stored separately and survive deploys.
# This actually happened: CONTACT_FROM_EMAIL was set as a Variable, worked, then
# vanished on the next deploy and every send failed with "email-not-configured".
# CONTACT_FROM_EMAIL is not sensitive (it is in every email header), but it must
# still be a Secret so that it persists.
# Routes return 503 "not_configured" until the FORMS binding above is real — safe
# to deploy before provisioning.
# ── Privacy Console search (/privacy/) ──
# The console runs in the browser and cannot fetch other sites (CORS), so
# /api/scan proxies searches and page reads for it. Either backend works —
# set ONE of these pairs as Secrets:
#
#   BRAVE_SEARCH_API_KEY (Secret) — https://api-dashboard.search.brave.com/
#       $5 of free credits each month (~1,000 searches). Preferred when both
#       are set: no daily cap and a much higher rate limit.
#
#   GOOGLE_CSE_KEY (Secret) — Custom Search JSON API key
#   GOOGLE_CSE_ID  (Secret) — search engine ID (cx), set to "search the entire
#       web" rather than a site list. 100 queries/day, no card required.
#
# /api/scan returns 503 "not_configured" until one backend is set, and the
# console falls back to its paste-a-results-page flow — so deploying without
# either changes nothing.
#
# scan.js logs nothing, stores nothing and touches no D1 binding: the queries
# carry the visitor's name and address, so they transit and are discarded.
#
# LB_SIGNING_KEY (HMAC for attempt tokens) is also a Secret:
#   wrangler secret put LB_SIGNING_KEY     # 32+ random bytes
# Do NOT define secrets here — wrangler vars override dashboard secrets.
#
# NOTE: The AI Mock Interview tool (/interview.app/mock-ai/) is Bring-Your-Own-Key —
# the visitor's browser calls the Anthropic API directly with their own key. It
# does NOT go through this Worker and costs the site owner nothing, so there is
# no ANTHROPIC_API_KEY secret to set here.
# NOTE: routeLeaderboard() returns 503 "not configured" until both the LB
# binding above is uncommented and LB_SIGNING_KEY is set — so deploying this
# now is safe and changes nothing about the live analytics until you opt in.
